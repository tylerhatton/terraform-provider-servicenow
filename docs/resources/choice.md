---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "servicenow_choice Resource - servicenow"
subcategory: ""
description: |-
  servicenow_choice manages a single choice list value (sys_choice entry) for a column on a table in ServiceNow.
---

# servicenow_choice (Resource)

`servicenow_choice` manages a single choice list value (sys_choice entry) for a column on a table in ServiceNow.

## Example Usage

```terraform
# Manages a choice list value (sys_choice entry) for a column on a ServiceNow table.
resource "servicenow_db_table" "example" {
  label     = "Example Table"
  user_role = ""
}

resource "servicenow_dictionary" "status" {
  name          = servicenow_db_table.example.name
  element       = "u_status"
  column_label  = "Status"
  internal_type = "string"
  max_length    = 40
  choice        = 3
}

resource "servicenow_choice" "open" {
  name     = servicenow_dictionary.status.name
  element  = servicenow_dictionary.status.element
  value    = "open"
  label    = "Open"
  sequence = 100
}

resource "servicenow_choice" "closed" {
  name     = servicenow_dictionary.status.name
  element  = servicenow_dictionary.status.element
  value    = "closed"
  label    = "Closed"
  sequence = 200
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `element` (String) The internal name of the column (field) this choice list value belongs to.
- `name` (String) The internal name of the table this choice list value belongs to.
- `value` (String) The stored value for this choice.

### Optional

- `dependent_value` (String) When this choice belongs to a dependent field, the value of the parent choice this depends on.
- `hint` (String) Tool tip text displayed when hovering over this choice.
- `inactive` (Boolean) If true, this choice is inactive and will not be displayed to users.
- `label` (String) The human readable label displayed for this choice.
- `language` (String) The language code this choice's label is written in.
- `sequence` (Number) Display order for this choice. Lower numbers appear first.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
- `read` (String)
- `update` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using the sys_id of the record in ServiceNow.
terraform import servicenow_choice.example <sys_id>
```
