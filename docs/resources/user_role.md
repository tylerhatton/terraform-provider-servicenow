---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "servicenow_user_role Resource - servicenow"
subcategory: ""
description: |-
  servicenow_user_role assigns a role to a user within ServiceNow.
---

# servicenow_user_role (Resource)

`servicenow_user_role` assigns a role to a user within ServiceNow.

## Example Usage

```terraform
# Assigns a role directly to a user.
resource "servicenow_user" "example" {
  user_name  = "jane.doe"
  first_name = "Jane"
  last_name  = "Doe"
}

resource "servicenow_role" "example" {
  suffix      = "example_role"
  description = "Example role managed by Terraform."
}

resource "servicenow_user_role" "example" {
  user = servicenow_user.example.id
  role = servicenow_role.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `role` (String) Sys ID of the role record being assigned.
- `user` (String) Sys ID of the user record receiving the role assignment.

### Optional

- `granted_by` (String) Sys ID of the record (such as a group) that granted this role to the user.
- `inherited` (Boolean) Whether this role was inherited from a group membership rather than directly assigned.
- `state` (String) Lifecycle state of the role assignment (typically 'active').

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using the sys_id of the record in ServiceNow.
terraform import servicenow_user_role.example <sys_id>
```
