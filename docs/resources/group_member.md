---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "servicenow_group_member Resource - servicenow"
subcategory: ""
description: |-
  servicenow_group_member manages the membership of a user in a user group within ServiceNow.
---

# servicenow_group_member (Resource)

`servicenow_group_member` manages the membership of a user in a user group within ServiceNow.

## Example Usage

```terraform
# Adds a user as a member of a group.
resource "servicenow_user" "example" {
  user_name  = "jane.doe"
  first_name = "Jane"
  last_name  = "Doe"
}

resource "servicenow_group" "example" {
  name        = "Example Engineering Group"
  description = "Engineering team responsible for example services."
}

resource "servicenow_group_member" "example" {
  user  = servicenow_user.example.id
  group = servicenow_group.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `group` (String) Sys ID of the user group record receiving the member.
- `user` (String) Sys ID of the user record being added to the group.

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using the sys_id of the record in ServiceNow.
terraform import servicenow_group_member.example <sys_id>
```
